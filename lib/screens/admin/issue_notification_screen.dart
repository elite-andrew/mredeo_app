import 'package:flutter/material.dart';

class IssueNotificationScreen extends StatelessWidget {
  const IssueNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Issue Notification')),
      body: const Center(child: Text('Issue Notification')),
    );
  }
}
