import 'package:flutter/material.dart';

class IssuePaymentScreen extends StatelessWidget {
  const IssuePaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Issue Payment')),
      body: const Center(child: Text('Issue Payment')),
    );
  }
}
